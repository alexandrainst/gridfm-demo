[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "gridfm-flower-server-deps"
version = "0.0.0"
dependencies = [
    "numpy>=2.5.2",
]

[tool.setuptools]
py-modules = []
